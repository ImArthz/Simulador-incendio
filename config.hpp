#if !defined(config)
#define config
#include <iostream>
class config
{
private:
    short int interaction_limit.
public:
    config(/* args */);
    ~config();
    void set_limit(short int interaction_limit);
    //função para checar se o animal ja foi no valor especifico da matriz talvez salvar em um vector 
    
};

#endif // MACRO

