#include "leitor.hpp"

#define input "dataset/mapa.dat"
#define output "dataset"/output.txt"

int main {
    Leitor *arquivo new leitor();
    // gerar mapa da funçao mapa 
    int interaction_count;
    arquivo ->setInput(input);
    arquivo->setoutput(output);
    // declarção para fazer o mapa 
    // função para setar as config's 
    //função iniciar 
    delete arquivo 
    // função delete mapa 
    return 0

}